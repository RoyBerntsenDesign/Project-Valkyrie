; called to return the probe to the docking station

G90                                 ; use absolute coordinates

;~~~~~ probe dock position ~~~~~
var Xdock_center = 31               ; probe dock X center position
var Ydock_center = -15              ; probe dock Y center position

;~~~~~ return probe if attached ~~~~~
if sensors.probes[0].value[0] != 1000  ; 1000 = probe not attached
    G1 X{var.Xdock_center} F30000       ; move to probe dock X position
    G1 Y{var.Ydock_center} F18000       ; move into probe dock
    G1 X70 F6000                        ; move slowly away to release probe

M98 P"/macros/probe_center"         ; move toolhead to bed center
M400                                ; wait until all positioning moves are complete